version = 1
identifier = "default"
description = "Capacités nécessaires pour Loop Visualizer"
windows = ["main"]
webviews = ["main"]
remote = { urls = ["http://localhost:1420/*"] }
permissions = [
  "core:default",
  "core:path:allow-basename",
  "dialog:default",
  "dialog:allow-open",
  "dialog:allow-save",
  "dialog:allow-message",
  "dialog:allow-ask",
  "dialog:allow-confirm",
  "shell:default",
  "shell:allow-open",
  { identifier = "shell:allow-spawn", allow = [ { name = "ffmpeg", sidecar = true, args = true } ] },
  "shell:allow-kill",
  "shell:allow-stdin-write",
  { identifier = "shell:allow-execute", allow = [ { name = "ffmpeg", sidecar = true, args = true } ] }
]
